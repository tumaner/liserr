export default class Account {
	constructor(email, password, name, birthday, sex, clocks) {
		this.email = email;
		this.password = password;
		this.name = name;
		this.birthday = birthday;
		this.sex = sex;
		this.clocks = clocks;
	}
}